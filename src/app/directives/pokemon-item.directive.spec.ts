import { PokemonItemDirective } from './pokemon-item.directive';

describe('PokemonItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonItemDirective();
    expect(directive).toBeTruthy();
  });
});
